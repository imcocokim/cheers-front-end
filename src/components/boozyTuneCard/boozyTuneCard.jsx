import { Link } from "react-router-dom";

const boozyTuneCard = ({}) => {
  return (
    <div className="card">
      <img 
        src={
        result.strTrackThumb
      } 
      alt={`${result.strTrack} album cover`}
      />
    </div>
  )
}

export default boozyTuneCard;